<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/aop
        http://www.springframework.org/schema/aop/spring-aop.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd">

    <!--开启基于bean管理自动扫描-->
    <context:component-scan base-package="com.kaishengit"/>
    <!--开启关于注解的aop 各种通知的-->
    <aop:aspectj-autoproxy/>

    <context:property-placeholder location="classpath:config.properties"/>
    <bean id="dateSource" class="org.apache.commons.dbcp2.BasicDataSource">
        <property name="driverClassName" value="${jdbc.driver}"/>
        <property name="url" value="${jdbc.url}"/>
        <property name="username" value="${jdbc.username}"/>
        <property name="password" value="${jdbc.password}"/>

        <property name="maxWaitMillis" value="${jdbc.maxsize}"/>
        <property name="initialSize" value="${jdbc.initsize}"/>
        <property name="maxTotal" value="${jdbc.maxsize}"/>
        <property name="maxIdle" value="${jdbc.mindel}"/>
        <property name="minIdle" value="${jdbc.mindel}"/>
    </bean>

    <!--创建JDBCTemplate-
    相当于DButil中的queryRuning对象，
    mybatiszhong的SqlSession对象
    执行增删改查的-->
    <bean id="jabcTemplate" class="org.springframework.jdbc.core.JdbcTemplate">
        <property name="dataSource" ref="dateSource"/>
        <!--能用set方法就不用构造方法
        <constructor-arg name="dataSource" ref="dateSource"/>-->
    </bean>





</beans>